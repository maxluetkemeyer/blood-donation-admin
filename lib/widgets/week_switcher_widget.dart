import 'package:flutter/material.dart';

class WeekSwitcher extends StatefulWidget {
  const WeekSwitcher({ Key? key }) : super(key: key);

  @override
  _WeekSwitcherState createState() => _WeekSwitcherState();
}

class _WeekSwitcherState extends State<WeekSwitcher> {
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}